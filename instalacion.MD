# Crear el entorno virtual en la carpeta .venv
python -m venv .venv


## Activar el entorno virtual

### PowerShell (terminal por defecto en Windows)

```powershell
.\.venv\Scripts\Activate.ps1
```

## Instalación
```sh
python -m pip install -r requirements.txt
```